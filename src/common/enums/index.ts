export enum Roles {
  USER = 'user',
  EMPLOYEE = 'employee',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
}

export enum RolesIds {
  user = 1,
  employee = 2,
  admin = 3,
  super_admin = 4,
}

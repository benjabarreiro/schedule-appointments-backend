export enum Roles {
  Patient = Number(process.env.Patient),
  Secretary = Number(process.env.Secretary),
  Doctor = Number(process.env.Doctor),
  Healthcare = Number(process.env.Healthcare),
  Administrator = Number(process.env.Administrator),
}
